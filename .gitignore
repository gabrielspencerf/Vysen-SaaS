# Dependencies
/node_modules
/.pnp
.pnp.js

# Build
/.next/
/out/
/build

# Env (nunca versionar segredos)
.env
.env*.local
.env.production
.env.production.local
stack.env
/pgdata

# Backups e dumps (podem conter PII e credenciais)
*.dump
*.sql.gz
*.rdb
dump.sql
**/pg_dump*.sql

# Docker stack (APENAS LOCAL / VPS — NUNCA subir no GitHub)
docker-stack.swarm.yml
docker-stack.swarm.app-only.yml
docker-stack.swarm.example.yml
docker-stack*.yml
*.stack.local.yml

# Debug / logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Uploads (arquivos enviados pelos tenants)
/uploads

# Cursor debug logs (local)
.cursor/debug*.log

# Claude Code (local settings/cache)
.claude/

# Runtime dev/worker logs e pids (capturados por scripts de boot)
tmp_app_dev*.log
tmp_app_dev*.txt
tmp_app_dev.pid
tmp_worker_dev*.log
tmp_worker_dev.pid

# Capturas de UX (screenshots e frames de video gerados localmente)
ux-all-prints/
ux-screenshots/
ux-video-frames/
ux-video-frames-extra/
ux-video-minute-marks/

# Backups locais
docs.zip

# Misc
.DS_Store
*.pem
.vercel
